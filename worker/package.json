{
  "name": "tally-share",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.51.1",
    "typescript": "~5.9.2",
    "vite": "^8.2.1",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "hono": "^4.13.1"
  }
}
