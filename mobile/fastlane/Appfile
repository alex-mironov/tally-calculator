app_identifier("app.tally-calculator")

# Apple Developer Team ID (shared across every app on this account).
team_id(ENV["FASTLANE_TEAM_ID"] || "2AMB7W8PVR")

# Auth is via an App Store Connect API key (see fastlane/.env), so no apple_id needed.
